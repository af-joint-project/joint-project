#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../smui-theme/smui-theme" "$@"
  ret=$?
else 
  node  "$basedir/../smui-theme/smui-theme" "$@"
  ret=$?
fi
exit $ret
